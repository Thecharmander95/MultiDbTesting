%p#notice= notice

%p
  %b Title:
  = @uploader.title
  - if @uploader.image.attached?
    = image_tag @uploader.image.variant(resize_to_limit: [400, 400])

= link_to 'Edit', edit_uploader_path(@uploader)
\|
= link_to 'Back', uploaders_path
